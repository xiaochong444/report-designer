import type { ReportTemplate } from '../template-model/types';
/** A page after pagination */
export interface PaginatedPage {
    id: string;
    width: number;
    height: number;
    bands: PaginatedBand[];
    pageNumber: number;
}
export interface PaginatedBand {
    id: string;
    type: string;
    /** Absolute Y position on the page */
    absoluteY: number;
    height: number;
    components: PaginatedComponent[];
}
export interface PaginatedComponent {
    id: string;
    type: string;
    absoluteX: number;
    absoluteY: number;
    width: number;
    height: number;
    content?: string;
    imageSrc?: string;
    barcodeValue?: string;
}
/**
 * Paginate a rendered render tree.
 * Splits bands across pages when they exceed the printable area.
 */
export declare function paginate(template: ReportTemplate, data: Record<string, any[]>): PaginatedPage[];
/** Compute total height of a paginated page */
export declare function paginatedPageHeight(page: PaginatedPage): number;
//# sourceMappingURL=index.d.ts.map